<Window x:Class="SnipSnap.Views.WindowSelectionOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowState="Maximized">

    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding CancelCommand}"/>
    </Window.InputBindings>

    <Grid>
        <!-- Semi-transparent overlay -->
        <Rectangle Fill="#40000000" MouseMove="OnMouseMove" MouseLeftButtonDown="OnMouseLeftButtonDown"/>

        <!-- Window highlight border -->
        <Canvas x:Name="HighlightCanvas">
            <Border x:Name="HighlightBorder"
                    BorderBrush="{DynamicResource AccentBrush}"
                    BorderThickness="3"
                    Background="#300078D4"
                    Visibility="Collapsed"
                    IsHitTestVisible="False"/>
        </Canvas>

        <!-- Instructions -->
        <Border Background="#E0000000"
                CornerRadius="0"
                Padding="16,10"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Margin="0,20,0,0">
            <TextBlock Text="Click on a window to capture it. Press ESC to cancel."
                       Foreground="White"
                       FontFamily="Segoe UI"
                       FontSize="14"/>
        </Border>
    </Grid>
</Window>
